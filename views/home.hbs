<section class="things">
    {{#each item}}
    <div class="item">
        <a href="{{ID}}" class="item-link">
            <img src="images/{{Image}}" alt="item image" class="item-image">
        </a>
        
        <div class="label">
            <h2>{{Title}}</h2>
        </div>
    </div>
    {{/each}}
</section>

<section class="lightbox">
    <span class="close">x</span>
        <div class="sidebar">
            <img src="" alt="larger bio image">
        </div>

        <div class="lb-content">
            <!-- render the database content here -->
        </div>
</section>